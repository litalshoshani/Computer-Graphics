package Transformations;

/**
 * Lital Shoshani
 * Uriah Ahrak
 * A convenient enum TransformationCase
 * which defines in which case we are at,
 * according to the type of the mouse event
 */
public enum TransformationCase {
    TRANSLATE, SCALE, ROTATE
}